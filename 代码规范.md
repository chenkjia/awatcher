# Awatcher 项目代码规范 (Coding Guidelines)

基于现有代码库的结构与实现，为了保持代码的一致性、可读性和可维护性，特制定本项目的代码规范。所有新加入的代码及对现有代码的修改均应遵循本规范。

## 1. 命名规范 (Naming Conventions)

遵循 Python PEP 8 命名规范：

- **包和模块名 (Packages/Modules)**: 简短的全小写字母，单词间可用下划线分隔。例如：`data_fetch`, `db_operations`, `stock_processor.py`。
- **类名 (Classes)**: 采用大驼峰命名法（PascalCase）。例如：`BaostockClient`, `StockModel`, `MongoClient`。
- **函数和方法名 (Functions/Methods)**: 采用小写字母加下划线分隔（snake_case）。例如：`get_stock_list()`, `update_daily_data()`, `setup_indexes()`。
- **变量名 (Variables)**: 采用小写字母加下划线分隔（snake_case）。例如：`start_date`, `stock_list`, `total_count`。
- **常量名 (Constants)**: 采用全大写字母加下划线分隔（UPPER_SNAKE_CASE）。例如：`COLLECTION_NAME = 'stocks'`。
- **私有属性/方法 (Private Members)**: 仅在类内部使用的属性和方法，前缀应加单下划线。例如：`_instance`, `_login()`, `_connect()`。

## 2. 注释与文档 (Docstrings & Comments)

- **模块级别注释**: 每个 `.py` 文件的开头必须包含模块级别的 Docstring，简要说明该模块的用途。
  ```python
  """
  BaoStock API客户端模块，提供股票数据获取功能
  """
  ```
- **类和方法注释**: 所有类和公开的函数/方法都必须包含 Docstring。
  ```python
  def update_day_line(cls, code, day_line_data):
      """更新股票日线数据"""
  ```
- **行内注释**: 对于复杂的业务逻辑（如增量更新的日期判断），应在代码上方添加简明扼要的行内注释（使用 `#`）。

## 3. 日志与打印 (Logging & Printing)

- **禁止使用 `print()`**: 生产代码中严禁使用 `print()` 输出信息。
- **统一使用自定义 Logger**: 必须通过 `from utils.logger import logger` 引入并使用统一的日志模块（基于 `loguru`）。
- **日志级别规范**:
  - `logger.info()`: 用于记录系统正常运行的关键节点，如“登录成功”、“成功获取 xx 条数据”。
  - `logger.debug()`: 用于记录详细的执行过程（如逐条更新数据时的日志），避免在生产环境刷屏。
  - `logger.warning()`: 用于记录预期之外但不会导致程序崩溃的情况，如“股票 xx 不存在，无法保存日线数据”。
  - `logger.error()`: 用于记录捕获到的异常或导致操作失败的严重错误。

## 4. 异常处理 (Error Handling)

- **避免裸露的 `except:`**: 捕获异常时应明确指定异常类型，或使用 `except Exception as e:` 并记录详细错误。
- **异常捕获范围**: 涉及外部 I/O 的操作（如 API 请求、数据库连接、文件读写）必须包裹在 `try...except` 块中。
- **错误抛出与中断**: 
  - 在底层模块（如 `data_fetch`）中，发生严重错误时应使用 `raise` 将异常向上抛出。
  - 在入口模块（如 `main.py`）中，捕获到顶层异常后可使用 `sys.exit(1)` 优雅退出，并在退出前记录 `logger.error()`。

## 5. 架构与设计模式 (Architecture & Patterns)

- **单例模式 (Singleton)**: 对于需要维持全局唯一连接状态的类（如 `MongoClient` 和 `BaostockClient`），继续沿用 `__new__` 方法实现单例模式，以节约连接资源。
- **无状态模型类**: 对于仅提供数据操作方法的类（如 `StockModel`, `StockProcessor`），推荐使用 `@classmethod` 或 `@staticmethod`，避免不必要的实例化。
- **职责分离**: 
  - `data_fetch` 仅负责请求 API 并返回原始/清洗后的字典列表，不直接操作数据库。
  - `db_operations` 仅负责数据库读写，不包含复杂的业务逻辑。
  - `data_processing` 负责调度，从 `data_fetch` 获取数据并调用 `db_operations` 写入。

## 6. 代码格式与风格 (Formatting & Style)

- **缩进**: 统一使用 4 个空格进行缩进，禁止使用 Tab。
- **最大行宽**: 建议单行代码长度不超过 120 个字符。过长的方法调用应换行对齐。
- **导入顺序 (Imports)**: 按照以下顺序分组导入，每组之间空一行：
  1. Python 标准库 (如 `import sys`, `from datetime import datetime`)
  2. 第三方库 (如 `import baostock as bs`, `from pymongo import MongoClient`)
  3. 项目本地模块 (如 `from utils.logger import logger`, `from config import config`)
- **空行**: 类定义之间、类的方法之间保留一个空行。模块级别的函数和类之间保留两个空行。

## 7. 数据库操作规范 (Database Operations)

- **避免硬编码配置**: 数据库连接信息必须通过 `config.get_mongodb_config()` 获取，禁止在代码中硬编码。
- **防重与增量逻辑**: 对于时间序列数据（如日线、小时线），写入前必须进行去重检查或采用增量更新策略（利用 `$push`, `$set`, `$unset` 等 MongoDB 原子操作），避免数据冗余。
- **安全断开**: 需要长期运行的进程在退出时，应确保调用相关的 `close()` 或 `logout()` 方法释放资源。
