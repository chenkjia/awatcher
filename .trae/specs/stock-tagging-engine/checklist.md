- [x] 数据库模型 (`StockModel`) 支持更新 `tags` 字段。
- [x] 存在可扩展的规则引擎基类 (`BaseRule`)，方便未来添加新规则。
- [x] MACD 规则 (`MacdRule`) 正确计算 DIF 和 DEA，并正确判定 DIF > DEA。
- [x] 独立的打标脚本能够仅获取 `code` 列表，有效降低初始内存占用。
- [x] 独立的打标脚本能够遍历股票，正确调用引擎并将标签保存至数据库。
